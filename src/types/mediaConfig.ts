export interface MediaTabVisibilityConfig {
  showFacebook: boolean;
  showInstagram: boolean;
  showYouTube: boolean;
}

export const DEFAULT_TAB_CONFIG: MediaTabVisibilityConfig = {
  showFacebook: true,
  showInstagram: true,
  showYouTube: true,
};
