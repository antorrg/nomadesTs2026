import type { IWork } from "../../../../types/work"

export const mockWork:IWork[] = [
    {
        id: 0,
        title: 'Seccion vacia',
        picture: 'No hallada',
        text: 'No hay datos aun',
        enabled: true
    }
]
